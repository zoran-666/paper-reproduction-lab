# Experiments

Store implementation code, configurations, and immutable run logs under one folder per paper. Every reported result must be traceable to a run ID, commit, configuration, seed, and environment.
